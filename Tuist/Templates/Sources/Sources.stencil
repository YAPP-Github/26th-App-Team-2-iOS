//
//  {{ name }}View.swift
//  Brake
//
//  Created by {{ env.USER }} on {{ now|date:"MM/dd/yy" }}.
//

import SwiftUI

struct {{ name }}View: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, Brake!")
        }
    }
}

#Preview {
    {{ name }}View()
}
